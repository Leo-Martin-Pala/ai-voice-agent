# LiveKit Agents with all plugins
livekit-agents[google,azure]~=1.2

# Noise cancellation plugin
livekit-plugins-noise-cancellation~=0.2

# Environment variables
python-dotenv

# HTTP requests for API calls
requests